import type { Metadata } from "next";
import "./globals.css";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";

export const metadata: Metadata = {
  title: "Starnberger See Property Review — A private-buyer research portal",
  description:
    "A data-driven research portal for a €5–10M primary-residence purchase at the Starnberger See. Ten-year market history, community deep-dives, lakefront premium analysis, macro and micro trends.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col">
        <SiteHeader />
        <main className="flex-1">{children}</main>
        <SiteFooter />
      </body>
    </html>
  );
}
